# Number Guessing Game (C)

## Overview
A simple C program where the computer generates a random number between 1 and 100, and the player tries to guess it.  
Demonstrates loops, conditionals, random number generation, and user input handling.

## Usage
Compile and run:

```bash
gcc number_guessing_game.c -o number_guessing_game
./number_guessing_game
